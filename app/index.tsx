import React from 'react';
import ChatScreen from '../src/screens/ChatScreen';

// App opens directly to chat interface (like other AI apps)
// The ChatScreen is the primary and only interface
export default function App() {
  return <ChatScreen />;
}